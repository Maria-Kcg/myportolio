
const Title = ({ title }) => {
    return (
        <h2 className="font-heading font-bold text-3xl md:text-4xl text-brown-700 text-center uppercase mb-12 tracking-wide">
            {title}
        </h2>
    )
}

export default Title;